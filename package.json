{
  "name": "nextjs-strapi-vercel-sample",
  "private": true,
  "packageManager": "pnpm@9.3.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "dependencies": {
    "@strapi/strapi": "5.16.1"
  }
}